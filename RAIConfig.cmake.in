# Compute paths
get_filename_component(RAI_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)
set(RAI_INCLUDE_DIRS "@CONF_INCLUDE_DIRS@")

# Our library dependencies (contains definitions for IMPORTED targets)
if(NOT TARGET foo AND NOT RAI_BINARY_DIR)
  include("${RAI_CMAKE_DIR}/RAI_Targets.cmake")
endif()

# These are IMPORTED targets created by RAITargets.cmake
set(RAI_LIBRARIES TARGETS)
set(RAI_INCLUDE_DIR ${CONF_INCLUDE_DIRS})